spring:
  application:
    name: member  # 注册中心服务名，务必声明在这里
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848  # nacos server 地址
        file-extension: yaml  # 配置中心文件后缀，默认 properties
        namespace: train      # nacos 命名空间